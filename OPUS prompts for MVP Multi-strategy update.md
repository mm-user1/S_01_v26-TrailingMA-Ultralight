## Анализ слабых мест и потенциальных проблем

### 🔴 Критичные проблемы:

1. PARAMETER_MAP жестко закодирован для S01
   - Оптимизатор не сможет работать с S03 параметрами
   - Решение: сделать PARAMETER_MAP динамическим для каждой стратегии
2. Разная логика размера позиций
   - S01: `risk = equity * risk% / stop_distance`
   - S03: `size = floor(equity / price / contract_size) * contract_size`
   - Решение: вынести расчет размера в метод стратегии
3. S03 не имеет стопов/тейков
   - Текущий `run_strategy` ожидает stop_price, target_price
   - Решение: сделать стопы опциональными в движке
4. CSV_COLUMN_SPECS жестко привязан к S01
   - Экспорт результатов сломается для S03
   - Решение: динамические колонки от стратегии

### 🟡 Средние проблемы:

1. UI формы в index.html статичные
   - Придется делать отдельные блоки для каждой стратегии
   - Для MVP это ОК, но не масштабируется
2. Кеширование в оптимизаторе
   - `_ma_cache`, `_lowest_cache` специфичны для S01
   - Решение: универсальный кеш индикаторов
3. WFA использует фиксированные параметры
   - Нужна адаптация под разные стратегии

### 🟢 Минорные проблемы:

1. Пресеты содержат параметры S01
   - Нужны разные схемы для разных стратегий
2. barstate.isconfirmed в S03
   - В Python это проверка на конец бара

## Обновленное решение для MVP

### Минимальные изменения для работы двух стратегий:

python

```python
# src/strategy_registry.py
class StrategyRegistry:
    @staticmethod
    def get_strategy(name: str):
        if name == "S01_TrailingMA":
            from .strategies.s01_trailing_ma import S01Strategy
            return S01Strategy()
        elif name == "S03_Reversal":
            from .strategies.s03_reversal import S03Strategy
            return S03Strategy()
        else:
            raise ValueError(f"Unknown strategy: {name}")
    
    @staticmethod
    def get_parameter_map(strategy_name: str):
        """Возвращает PARAMETER_MAP для конкретной стратегии"""
        if strategy_name == "S01_TrailingMA":
            return S01_PARAMETER_MAP
        elif strategy_name == "S03_Reversal":
            return S03_PARAMETER_MAP
    
    @staticmethod
    def get_csv_columns(strategy_name: str):
        """Возвращает CSV_COLUMN_SPECS для конкретной стратегии"""
        # Аналогично
```

### Адаптация run_strategy:

python

~~~python
def run_strategy_universal(df, strategy, params, trade_start_idx=0):
    """Универсальный движок с поддержкой разных типов стратегий"""
    
    strategy_type = strategy.get_type()  # "stops_based" или "reversal"
    
    if strategy_type == "stops_based":
        # Текущая логика для S01
        return run_strategy_with_stops(df, strategy, params, trade_start_idx)
    elif strategy_type == "reversal":
        # Упрощенная логика для S03
        return run_strategy_reversal(df, strategy, params, trade_start_idx)
```

## Промпты для агента-кодера

Да, это отличная идея! Вот структурированные промпты:

### Промпт 1: Подготовка инфраструктуры
```
ЗАДАЧА: Создать базовую инфраструктуру для поддержки множественных стратегий

КОНТЕКСТ:
- Сейчас стратегия S01_TrailingMA жестко закодирована в backtest_engine.py
- Нужно добавить поддержку стратегии S03_Reversal и возможность добавления новых

ДЕЙСТВИЯ:
1. Создать папку /src/strategies/
2. Создать файл /src/indicators.py и перенести туда все функции индикаторов из backtest_engine.py (sma, ema, hma, wma, vwma, alma, dema, kama, tma, t3, vwap, get_ma, atr)
3. Создать /src/strategies/base.py с базовым классом:
   - BaseParams (dataclass) с общими параметрами
   - BaseStrategy (ABC) с абстрактными методами
4. Создать /src/strategy_registry.py для управления стратегиями

ВАЖНО:
- Сохранить обратную совместимость - старый код должен работать
- Не ломать импорты в других файлах
```

### Промпт 2: Миграция S01
```
ЗАДАЧА: Вынести стратегию S01_TrailingMA в отдельный модуль

КОНТЕКСТ:
- Логика стратегии находится в backtest_engine.py в функции run_strategy()
- Параметры в классе StrategyParams

ДЕЙСТВИЯ:
1. Создать /src/strategies/s01_trailing_ma.py
2. Создать класс S01Params(BaseParams) со всеми специфичными параметрами
3. Создать класс S01Strategy(BaseStrategy)
4. Перенести логику из run_strategy() с минимальными изменениями
5. В backtest_engine.py оставить алиасы для обратной совместимости:
   - StrategyParams = S01Params
   - run_strategy должен вызывать S01Strategy.run()

КРИТИЧНО:
- Результаты бэктеста должны быть ИДЕНТИЧНЫ до и после рефакторинга
- Все тесты должны проходить
```

### Промпт 3: Добавление S03
```
ЗАДАЧА: Реализовать стратегию S03_Reversal на Python

ИСХОДНЫЙ КОД: S_03_Reversal_v07_Light_for_PROJECT_PLAN.pine

КЛЮЧЕВЫЕ ОТЛИЧИЯ S03:
- НЕТ стопов и тейк-профитов
- Размер позиции = floor(equity / close / contract_size) * contract_size
- Выход только по реверсу сигнала или при выходе из timeInRange
- Условия входа основаны на пересечении MA

ДЕЙСТВИЯ:
1. Создать /src/strategies/s03_reversal.py
2. Создать S03Params с параметрами:
   - use_ma1, ma_type1, ma_length1
   - use_ma2, ma_type2, ma_length2  
   - use_ma3, ma_type3, ma_length3
   - use_close_count, close_count_long, close_count_short
   - breakout_mode, use_close_price
3. Реализовать логику:
   - Расчет 3х MA
   - Подсчет close_count относительно MA3
   - Вход: когда close пересекает MA (направление зависит от breakout_mode)
   - Выход: реверс сигнала или выход из временного диапазона

ВАЖНО:
- В Pine используется barstate.isconfirmed - в Python это последний бар
- strategy.position_size[1] == 0 - проверка что на прошлом баре не было позиции
```

### Промпт 4: Адаптация оптимизатора
```
ЗАДАЧА: Сделать оптимизатор совместимым с разными стратегиями

ПРОБЛЕМА:
- PARAMETER_MAP и CSV_COLUMN_SPECS жестко закодированы для S01
- Оптимизатор не знает о параметрах S03

ДЕЙСТВИЯ:
1. В strategy_registry.py добавить методы:
   - get_parameter_map(strategy_name) - возвращает маппинг для стратегии
   - get_csv_columns(strategy_name) - возвращает колонки для экспорта
2. В optimizer_engine.py:
   - Добавить параметр strategy_name в OptimizationConfig
   - Использовать динамический PARAMETER_MAP из registry
   - Адаптировать generate_parameter_grid()
3. В server.py:
   - Добавить выбор стратегии в /run-optimization endpoint
   - Передавать strategy_name в OptimizationConfig

ДЛЯ MVP:
- Можно использовать if/else для двух стратегий
- Главное чтобы работало, рефакторинг потом
```

### Промпт 5: Обновление UI
```
ЗАДАЧА: Добавить выбор стратегии в интерфейс

ДЕЙСТВИЯ:
1. В index.html добавить dropdown для выбора стратегии:
   - S01_TrailingMA (default)
   - S03_Reversal
2. При смене стратегии:
   - Показывать/скрывать соответствующие блоки параметров
   - Для S01 - текущие параметры
   - Для S03 - упрощенный набор (3 MA, close counts, breakout)
3. В server.py:
   - Endpoint /api/strategies - список доступных стратегий
   - В /run-backtest принимать параметр strategy_name
   - Создавать соответствующие Params и Strategy

ДЛЯ MVP:
- Можно хардкодить два блока параметров и переключать visibility
- Динамическая генерация форм - это для v2
```

### Промпт 6: Финальное тестирование
```
ЗАДАЧА: Протестировать что все работает корректно

ТЕСТЫ:
1. S01_TrailingMA:
   - Запустить бэктест с теми же параметрами что и раньше
   - Результаты должны быть ИДЕНТИЧНЫ
   - Оптимизация Grid должна работать
   - Оптимизация Optuna должна работать
   - WFA должен работать

2. S03_Reversal:
   - Запустить бэктест с параметрами из Pine
   - Проверить что позиции открываются/закрываются правильно
   - Оптимизация должна работать с параметрами S03

3. Переключение:
   - UI корректно переключает формы
   - Пресеты сохраняются отдельно для каждой стратегии
   - Экспорт результатов работает для обеих стратегий

КРИТИЧНО:
- Если что-то сломалось для S01 - откатить изменения
- S01 должна работать как минимум не хуже чем раньше
~~~

## Итоговые рекомендации

1. **Делайте пошагово** - каждый промпт это отдельный PR/коммит
2. **Тестируйте после каждого шага** - не накапливайте ошибки
3. **Для MVP используйте if/else** - абстракции потом
4. **Сохраняйте обратную совместимость** - старые скрипты должны работать
5. **Документируйте изменения** - особенно breaking changes