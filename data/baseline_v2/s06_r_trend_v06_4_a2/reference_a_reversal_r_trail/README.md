# Reference A: Reversal + R Trail

Accepted TradingView reference using Reversal entry, R Trail, activation `1R`,
distance `2R`, Stop X `2`, Stop LP `2`, Stop Max `4%`, and Max Days `6`.

The export contains 52 closed trades and matches the metrics screenshot. Eight
profitable method-controlled exits cover four Long and four Short trail exits.
The final position closes naturally before the End Date.
