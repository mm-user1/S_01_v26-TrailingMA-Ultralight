# Reference B: Trend + R Trail

Accepted TradingView reference using Trend entry, R Trail, activation `1R`,
distance `2.5R`, Stop X `2.5`, Stop LP `2`, Stop Max `8%`, and Max Days `4`.

The export contains 42 closed trades and matches the metrics screenshot. Three
profitable method-controlled exits cover two Long and one Short trail exit. The
final position closes naturally before the End Date.
